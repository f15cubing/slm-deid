# Core training stack (QLoRA fine-tuning of Qwen3-1.7B)
# Pin exact versions on Day 1 once the GPU environment is stood up.
# unsloth + bitsandbytes are CUDA-only; on macOS the hf/MPS backend is used instead, so they are
# gated to Linux (Colab) and skipped by `pip install -r requirements.txt` on a Mac.
unsloth; platform_system == "Linux"
transformers>=4.44
datasets>=2.20
trl>=0.9
peft>=0.12
accelerate>=0.33
bitsandbytes>=0.43; platform_system == "Linux"
torch>=2.3

# Data-gen teacher client (frontier model distillation) — pick one
openai>=1.40
anthropic>=0.34

# Pipeline / negatives / eval
presidio-analyzer
faker
pyyaml>=6.0

# Publishing (model + dataset to the HuggingFace Hub — scripts/push_*.py)
huggingface_hub>=0.24

# Dev / test
pytest>=8.2
ruff>=0.6
